import { Meta } from '@storybook/addon-docs';

<Meta title="Welcome"
      parameters={{
        viewport: { disable: true },
        viewMode: 'docs',
        previewTabs: { canvas: { hidden: true } }
      }}
/>

# Vue Components Showcase
Showcase of Vue components built for RiseUp's design system.
The components were written in Vue.js (2), but some are compatible with Vue 3.
