export const Contributor = ({ name, img, position }) => {
  return (
    <div style={{ display: 'flex', flexDirection: 'column', justifyContent: 'center', alignItems: 'center' }}>
      <img src={img}
           style={{
             height: 120,
             width: 120,
             borderRadius: 100,
             boxShadow: '1px 1px 2px 0px #c5c5c5',
             objectFit: 'cover'
           }}/>
      <div style={{ marginTop: 8, fontWeight: 500 }}>{name}</div>
      <div style={{ fontSize: 12 }}>{position}</div>
    </div>
  );
};
